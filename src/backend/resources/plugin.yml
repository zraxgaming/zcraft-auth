name: AuthBackend
version: '1.0.0'
main: xyz.zcraft.studio.auth.backend.BackendAuthPlugin
description: "Small backend auth guard for Spigot/Paper/Purpur/Folia servers behind Auth proxy."

permissions:
  zcraftauth.backend.bypass:
    description: Bypass backend auth guard
    default: false
